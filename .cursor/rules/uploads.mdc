---
description: UploadThing file upload rules, router setup, and DB sync patterns
globs: ["app/api/uploadthing/**", "lib/uploadthing.ts", "components/shared/FileUploader.tsx"]
---

# File Upload Rules (UploadThing)

## What gets uploaded
- Research PDFs (max 16MB, up to 5 per project)
- Poster/project images (max 8MB, up to 10 per project)
- Uploaded by teachers on the public `/submit/[eventId]` page — no auth required

## Always Store These Three Fields
When a file upload completes, always save to `ProjectFile`:
- `url` — for displaying/downloading
- `key` — for deleting from UploadThing storage later
- `size` — for display and storage monitoring

Never store just the URL. Without the `key` you cannot delete the file.

## Router (app/api/uploadthing/core.ts)
```ts
import { createUploadthing, type FileRouter } from "uploadthing/next"

const f = createUploadthing()

export const ourFileRouter = {
  projectFiles: f({
    pdf: { maxFileSize: "16MB", maxFileCount: 5 },
    image: { maxFileSize: "8MB", maxFileCount: 10 },
  })
    .input(z.object({ projectId: z.string() }))
    .middleware(async ({ input }) => {
      // Public upload — no auth check here
      // projectId passed from client to link files to the right project
      return { projectId: input.projectId }
    })
    .onUploadComplete(async ({ metadata, file }) => {
      await prisma.projectFile.create({
        data: {
          name: file.name,
          url: file.url,
          key: file.key,
          size: file.size,
          type: file.type,
          projectId: metadata.projectId,
        },
      })
    }),
} satisfies FileRouter

export type OurFileRouter = typeof ourFileRouter
```

## ENV Variables
```env
UPLOADTHING_SECRET=sk_live_...
UPLOADTHING_APP_ID=...
```

## Deleting Files
Always use the stored `key` to delete from UploadThing:
```ts
import { UTApi } from "uploadthing/server"

const utapi = new UTApi()

export async function deleteProjectFile(fileId: string) {
  const file = await prisma.projectFile.findUniqueOrThrow({ where: { id: fileId } })
  await utapi.deleteFiles(file.key)
  await prisma.projectFile.delete({ where: { id: fileId } })
}
```
